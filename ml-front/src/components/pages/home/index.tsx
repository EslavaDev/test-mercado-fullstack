import React from 'react';
import { Layout } from '../../template/layout';

export const HomeScreen = () => {
  return (
    <Layout title="Home" description="page for see input search">
      {null}
    </Layout>
  );
};
